#pragma once

// This enum will be used to tell lanes which types of cars to spawn
// Some of these will refer to different classes, but some will refer to the
// same class with different initialization parameters (different sprite, size, etc.)

enum class carType : int
{
  REGULAR
};
